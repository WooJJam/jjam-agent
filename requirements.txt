# jjam-agent 도구 스크립트 의존성
#
# 원칙: scripts/*.py 는 Python 3.12 표준 라이브러리만으로 동작하도록 작성되어
# 있어, 아무것도 설치하지 않아도 실행됩니다. 아래는 선택(권장) 의존성입니다.
#
# 설치: py -m pip install -r requirements.txt   (Linux/EC2: python3 -m pip install -r requirements.txt)

# config/sources.yaml 을 더 견고하게 파싱 (미설치 시 collect-news.py 내장 fallback 파서 사용)
PyYAML>=6.0

# 로컬에서 .env 를 셸에 로드하지 않고 스크립트가 직접 읽고 싶을 때 (선택)
# 운영(Hermes)에서는 ~/.hermes/.env 를 Hermes가 관리하므로 불필요
python-dotenv>=1.0 ; python_version >= "3.8"
